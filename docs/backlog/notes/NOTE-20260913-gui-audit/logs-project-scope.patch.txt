diff --git a/src/features/logs/logs-dashboard.tsx b/src/features/logs/logs-dashboard.tsx
--- a/src/features/logs/logs-dashboard.tsx
+++ b/src/features/logs/logs-dashboard.tsx
@@ -5,7 +5,8 @@
 import { Alert, AlertDescription } from "@/components/ui/alert";
 import { Badge } from "@/components/ui/badge";
 import { Button } from "@/components/ui/button";
-import { Card, CardContent } from "@/components/ui/card";
+import { Card } from "@/components/ui/card";
+import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible";
 import { Input } from "@/components/ui/input";
 import { Label } from "@/components/ui/label";
 import { ScrollArea } from "@/components/ui/scroll-area";
@@ -16,33 +17,30 @@

 export function LogsDashboard({ snapshots, aggregate }: { snapshots: ProjectSnapshot[]; aggregate: boolean }) {
   const { t } = useI18n();
-  const [sourceId, setSourceId] = useState(snapshots.find((s) => s.runtime.phase === "running")?.project.id ?? snapshots[0]?.project.id);
-  const selected = snapshots.find((s) => s.project.id === sourceId) ?? snapshots[0];
-  if (!selected) return null;
+  const [query, setQuery] = useState("");
   return <section data-logs-dashboard className="min-w-0 space-y-4" aria-label={t("project.logs")}>
     <p className="text-sm text-muted-foreground">{t("logsView.lead")}</p>
-    <div className={aggregate ? "grid min-w-0 gap-4 xl:grid-cols-[13rem_minmax(0,1fr)]" : "min-w-0"}>
-      {aggregate ? <Card className="min-w-0 gap-0 self-start py-3 shadow-none"><CardContent className="px-3">
-        <h3 className="mb-2 px-2 text-xs font-medium text-muted-foreground">{t("logsView.sources")}</h3>
-        <nav aria-label={t("logsView.sources")} className="flex max-h-44 gap-1 overflow-auto xl:max-h-[65dvh] xl:flex-col">{snapshots.map((snapshot) => <Button key={snapshot.project.id} variant="ghost" aria-pressed={snapshot.project.id === selected.project.id} onClick={() => setSourceId(snapshot.project.id)} className="h-auto min-w-40 flex-col items-start gap-2 whitespace-normal p-3 text-left aria-pressed:bg-accent xl:min-w-0 xl:w-full">
-          <span className="max-w-full break-words text-sm">{snapshot.project.name}</span><RuntimeBadge phase={snapshot.runtime.phase} />
-        </Button>)}</nav>
-      </CardContent></Card> : null}
-      <LogConsole key={JSON.stringify([selected.project.id, selected.runtime.startedAt, selected.runtime.worktreePath])} snapshot={selected} />
-    </div>
+    <div className="space-y-1"><Label htmlFor="log-search">{t(aggregate ? "logsView.searchAll" : "logsView.search")}</Label><div className="relative"><Search className="absolute top-2 left-2 size-4 text-muted-foreground" aria-hidden /><Input id="log-search" type="search" className="pl-8" value={query} onChange={(event) => setQuery(event.target.value)} /></div></div>
+    {snapshots.map((snapshot) => <LogConsole key={JSON.stringify([snapshot.project.id, snapshot.runtime.startedAt, snapshot.runtime.worktreePath])} snapshot={snapshot} aggregate={aggregate} query={query} clearQuery={() => setQuery("")} />)}
   </section>;
 }

-function LogConsole({ snapshot }: { snapshot: ProjectSnapshot }) {
+function LogConsole({ snapshot, aggregate, query, clearQuery }: { snapshot: ProjectSnapshot; aggregate: boolean; query: string; clearQuery: () => void }) {
   const { t, locale } = useI18n();
   const raw = snapshot.runtime.logs;
-  const [paused, setPaused] = useState<string[] | null>(null);
-  const [query, setQuery] = useState("");
+  const [paused, setPaused] = useState<string[] | null>(query ? [...raw] : null);
+  const [previousQuery, setPreviousQuery] = useState(query);
+  const [expanded, setExpanded] = useState(Boolean(query) || snapshot.runtime.phase !== "stopped");
   const [hit, setHit] = useState(0);
   const [wrap, setWrap] = useState(true);
   const [notice, setNotice] = useState<string | null>(null);
   const [error, setError] = useState<string | null>(null);
   const viewport = useRef<HTMLDivElement>(null);
+  if (previousQuery !== query) {
+    setPreviousQuery(query);
+    setHit(0);
+    if (query) { setExpanded(true); if (paused === null) setPaused([...raw]); }
+  }
   const lines = useMemo(() => (paused ?? raw).map(cleanLogText), [paused, raw]);
   const content = lines.join("\n");
   const { matches, truncated } = useMemo(() => logMatches(lines, query), [lines, query]);
@@ -55,7 +53,7 @@

   useEffect(() => {
     if (paused === null && viewport.current) viewport.current.scrollTop = viewport.current.scrollHeight;
-  }, [content, paused, wrap]);
+  }, [content, paused, wrap, expanded]);

   useEffect(() => {
     if (!query || !matches.length || !viewport.current) return;
@@ -64,9 +62,9 @@
       viewport.current.scrollTop += mark.getBoundingClientRect().top - viewport.current.getBoundingClientRect().top - viewport.current.clientHeight / 2;
       if (!wrap) viewport.current.scrollLeft += mark.getBoundingClientRect().left - viewport.current.getBoundingClientRect().left - viewport.current.clientWidth / 2;
     }
-  }, [query, currentHit, matches.length, wrap]);
+  }, [query, currentHit, matches.length, wrap, expanded]);

-  const resume = () => { setQuery(""); setHit(0); setPaused(null); };
+  const resume = () => { clearQuery(); setHit(0); setPaused(null); };
   const move = (delta: number) => setHit((currentHit + delta + matches.length) % Math.max(matches.length, 1));
   const copy = async () => {
     setError(null); setNotice(null);
@@ -80,14 +78,14 @@
     document.body.append(anchor); anchor.click(); anchor.remove(); URL.revokeObjectURL(url);
   };

-  return <Card className="min-w-0 gap-0 overflow-hidden py-0 shadow-none" data-log-console>
+  return <Collapsible open={!aggregate || expanded} onOpenChange={setExpanded} asChild><Card className="min-w-0 gap-0 overflow-hidden py-0 shadow-none" data-log-console data-project-id={snapshot.project.id}>
     <div className="space-y-2 border-b px-4 py-3">
-      <div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold">{snapshot.project.name}</h3><RuntimeBadge phase={snapshot.runtime.phase} /><Badge variant="secondary">{t(active ? "logsView.currentRun" : "logsView.lastRun")}</Badge></div>
+      <div className="flex flex-wrap items-center gap-2">{aggregate ? <CollapsibleTrigger asChild><Button variant="ghost" size="icon-sm" aria-label={t(expanded ? "logsView.collapse" : "logsView.expand", { project: snapshot.project.name })}>{expanded ? <ChevronUp aria-hidden /> : <ChevronDown aria-hidden />}</Button></CollapsibleTrigger> : null}<h3 className="font-semibold">{snapshot.project.name}</h3><RuntimeBadge phase={snapshot.runtime.phase} /><Badge variant="secondary">{t(active ? "logsView.currentRun" : snapshot.runtime.startedAt ? "logsView.lastRun" : "logsView.noRun")}</Badge></div>
       <p className="break-all font-mono text-xs text-muted-foreground">{worktree?.branch ?? snapshot.runtime.worktreePath ?? t("logsView.noRun")} · {t("project.port")} {snapshot.project.port}</p>
       {snapshot.runtime.startedAt ? <p className="text-xs text-muted-foreground">{t("logsView.started", { time: new Date(snapshot.runtime.startedAt).toLocaleString(locale) })}</p> : null}
     </div>
+    <CollapsibleContent>
     <div className="flex flex-wrap items-end gap-2 border-b p-3">
-      <div className="min-w-0 flex-1 basis-44 space-y-1"><Label htmlFor="log-search" className="text-xs">{t("logsView.search")}</Label><div className="relative"><Search className="absolute top-2 left-2 size-4 text-muted-foreground" aria-hidden /><Input id="log-search" type="search" className="pl-8" value={query} onChange={(event) => { setQuery(event.target.value); setHit(0); if (paused === null) setPaused([...raw]); }} onKeyDown={(event) => { if (event.key === "Enter") { event.preventDefault(); move(event.shiftKey ? -1 : 1); } }} /></div></div>
       <span className="min-w-12 pb-2 text-center text-xs text-muted-foreground" role="status">{query ? `${matches.length ? currentHit + 1 : 0}/${matches.length}${truncated ? "+" : ""}` : ""}</span>
       <Button variant="outline" size="icon-sm" disabled={!matches.length} aria-label={t("logsView.previous")} title={t("logsView.previous")} onClick={() => move(-1)}><ChevronUp aria-hidden /></Button>
       <Button variant="outline" size="icon-sm" disabled={!matches.length} aria-label={t("logsView.next")} title={t("logsView.next")} onClick={() => move(1)}><ChevronDown aria-hidden /></Button>
@@ -98,7 +96,7 @@
     </div>
     {error ? <Alert variant="destructive" className="rounded-none"><AlertDescription>{error}</AlertDescription></Alert> : null}
     {notice ? <p role="status" className="px-4 py-2 text-xs text-muted-foreground">{notice}</p> : null}
-    <ScrollArea className="h-[max(22rem,calc(100dvh-25rem))] min-w-0 bg-muted/30" horizontal={!wrap} viewportRef={viewport} viewportProps={{ tabIndex: 0, role: "region", "aria-label": t("logsView.console"), onScroll: () => {
+    <ScrollArea className={`${aggregate ? "h-72" : "h-[max(22rem,calc(100dvh-25rem))]"} min-w-0 bg-muted/30`} horizontal={!wrap} viewportRef={viewport} viewportProps={{ tabIndex: 0, role: "region", "aria-label": t("logsView.console"), onKeyDown: (event) => { if (query && event.key === "Enter") { event.preventDefault(); move(event.shiftKey ? -1 : 1); } }, onScroll: () => {
       const element = viewport.current;
       if (!element) return;
       const atBottom = element.scrollHeight - element.clientHeight - element.scrollTop < 24;
@@ -120,5 +118,6 @@
       <p className="text-xs text-muted-foreground">{t("logsView.retained", { count: lines.length })}{paused !== null ? ` · ${t("logsView.paused")}` : ""}{truncated ? ` · ${t("logsView.matchLimit")}` : ""}</p>
       {paused !== null ? <Button size="sm" variant={hasNew ? "default" : "outline"} onClick={resume}><ArrowDown aria-hidden />{t(hasNew ? "logsView.newEntries" : "logsView.bottom")}</Button> : null}
     </div>
-  </Card>;
+    </CollapsibleContent>
+  </Card></Collapsible>;
 }
diff --git a/tests/ui/logs.spec.ts b/tests/ui/logs.spec.ts
--- a/tests/ui/logs.spec.ts
+++ b/tests/ui/logs.spec.ts
@@ -12,7 +12,7 @@
     const first = data.projects[0];
     first.runtime.phase = "running"; first.runtime.worktreePath = first.worktrees[0].path; first.runtime.startedAt = "2026-09-13T12:00:00Z";
     first.runtime.logs = ["\u001b[31mERROR one\u001b[0m", "context line", "error two", "literal [a].*", "<script>window.injected=true</script>", "long " + "x".repeat(3000)];
-    const other = structuredClone(first); other.project.id = "api"; other.project.name = "Fixture API"; other.runtime.phase = "stopped"; other.runtime.logs = ["older-api-output"];
+    const other = structuredClone(first); other.project.id = "api"; other.project.name = "Fixture API"; other.runtime.phase = "stopped"; other.runtime.logs = ["older-api-output", "error api"];
     data.projects.push(other);
     const { requests, errors } = await mountDashboard(page, data);
     await page.getByRole("combobox", { name: /Choose project/ }).click();
@@ -20,19 +20,27 @@
     if (width < 768) await page.getByRole("button", { name: "Toggle navigation", exact: true }).click();
     await page.getByRole("navigation").getByRole("button", { name: "Logs", exact: true }).click();
     const screen = page.locator("[data-logs-dashboard]");
-    const console = page.locator("[data-log-console]");
+    const console = page.locator('[data-log-console][data-project-id="web"]');
+    const otherConsole = page.locator('[data-log-console][data-project-id="api"]');
+    const search = screen.getByRole("searchbox");
+    await expect(page.locator("[data-log-console]")).toHaveCount(2);
+    await expect(screen.getByRole("navigation")).toHaveCount(0);
+    await expect(otherConsole.getByRole("button", { name: "Expand logs: Fixture API" })).toHaveAttribute("aria-expanded", "false");
+    await expect(otherConsole.locator("[data-log-lines]")).toHaveCount(0);
     const text = console.locator("[data-log-lines]");
     await expect(console).toContainText("Current run");
     await expect(text).toContainText("ERROR one");
     expect(await text.textContent()).not.toContain("\u001b");
     expect(await page.evaluate(() => (window as unknown as { injected?: boolean }).injected)).toBeUndefined();
-    await console.getByRole("searchbox", { name: "Search logs" }).fill("error");
+    await search.fill("error");
     await expect(console.locator("mark")).toHaveCount(2);
+    await expect(otherConsole.locator("mark")).toHaveCount(1);
+    await expect(otherConsole.getByRole("button", { name: "Collapse logs: Fixture API" })).toHaveAttribute("aria-expanded", "true");
     await expect(console.locator('[data-current-hit="true"]')).toHaveText("ERROR");
     await console.getByRole("button", { name: "Next match", exact: true }).click();
     await expect(console.locator('[data-current-hit="true"]')).toHaveText("error");
     await expect(text).toContainText("context line");
-    await console.getByRole("searchbox").fill("[a].*");
+    await search.fill("[a].*");
     await expect(console.locator("mark")).toHaveCount(1);
     await console.getByRole("button", { name: "Copy displayed buffer", exact: true }).click();
     expect(await page.evaluate(() => (window as unknown as { copiedLogs: string }).copiedLogs)).toContain("ERROR one\ncontext line\nerror two");
@@ -44,19 +52,24 @@
     await console.getByRole("button", { name: "Wrap lines", exact: true }).click();
     await expect(console.getByRole("button", { name: "Wrap lines", exact: true })).toHaveAttribute("aria-pressed", "false");
     expect(await page.evaluate(() => document.documentElement.scrollWidth)).toBeLessThanOrEqual(width);
-    await screen.getByRole("navigation", { name: "Log sources" }).getByRole("button", { name: /Fixture API/ }).click();
-    await expect(console).toContainText("Last run logs");
-    await expect(text).toContainText("older-api-output");
-    await expect(text).not.toContainText("ERROR one");
-    await expect(console.getByRole("searchbox")).toHaveValue("");
+    await expect(otherConsole).toContainText("Last run logs");
+    await expect(otherConsole.locator("[data-log-lines]")).toContainText("older-api-output");
+    await expect(otherConsole.locator("[data-log-lines]")).not.toContainText("ERROR one");
+    await search.fill("");
+    await otherConsole.getByRole("button", { name: "Collapse logs: Fixture API" }).click();
+    await expect(otherConsole.locator("[data-log-lines]")).toHaveCount(0);
+    await otherConsole.getByRole("button", { name: "Expand logs: Fixture API" }).focus();
+    await page.keyboard.press("Enter");
+    await expect(otherConsole.locator("[data-log-lines]")).toBeVisible();
     await page.screenshot({ path: test.info().outputPath("logs.png"), fullPage: true, animations: "disabled" });
     await page.getByRole("combobox", { name: /Choose project/ }).click();
     await page.getByRole("option", { name: /Fixture Web/ }).click();
-    await expect(screen.getByRole("navigation", { name: "Log sources" })).toHaveCount(0);
+    await expect(page.locator("[data-log-console]")).toHaveCount(1);
+    await expect(console.getByRole("button", { name: /Collapse logs|Expand logs/ })).toHaveCount(0);
     await expect(text).toContainText("ERROR one");
     await expect(console.getByRole("button", { name: "Start", exact: true })).toHaveCount(0);
     await page.getByRole("button", { name: "Switch language to Polish", exact: true }).click();
-    await expect(console.getByRole("searchbox", { name: "Szukaj w logach" })).toBeVisible();
+    await expect(screen.getByRole("searchbox", { name: "Szukaj w logach" })).toBeVisible();
     expect(requests).toEqual([]); expect(errors).toEqual([]);
   });
 }
@@ -84,7 +97,7 @@
   await expect(console.locator("[data-log-lines]")).toContainText("new-line-at-the-end");
   await expect.poll(bottomDistance).toBeLessThan(24);
   await page.screenshot({ path: test.info().outputPath("logs-live.png"), animations: "disabled" });
-  await console.getByRole("searchbox").fill("not-present");
+  await page.getByRole("searchbox", { name: "Search logs" }).fill("not-present");
   await expect(console.getByRole("button", { name: "Next match" })).toBeDisabled();
   await expect(console.locator("[data-log-lines]")).toContainText("original-line-399");
   await expect.poll(() => page.evaluate(() => (window as unknown as { fixtureEvents: { active: number } }).fixtureEvents.active)).toBe(1);
diff --git a/src/i18n/messages.ts b/src/i18n/messages.ts
--- a/src/i18n/messages.ts
+++ b/src/i18n/messages.ts
@@ -259,7 +259,9 @@
   "project.branch": "Gałąź",
   "project.started": "Uruchomiony",
   "logsView.lead": "Czytaj logi serwera, wyszukuj komunikaty i śledź nowe wpisy.",
-  "logsView.sources": "Źródła logów",
+  "logsView.searchAll": "Szukaj w logach wszystkich projektów",
+  "logsView.expand": "Rozwiń logi: {project}",
+  "logsView.collapse": "Zwiń logi: {project}",
   "logsView.currentRun": "Bieżące uruchomienie",
   "logsView.lastRun": "Logi ostatniego uruchomienia",
   "logsView.noRun": "Brak uruchomienia",
@@ -759,7 +761,9 @@
   "project.branch": "Branch",
   "project.started": "Started",
   "logsView.lead": "Read server output, search messages and follow new entries.",
-  "logsView.sources": "Log sources",
+  "logsView.searchAll": "Search logs across all projects",
+  "logsView.expand": "Expand logs: {project}",
+  "logsView.collapse": "Collapse logs: {project}",
   "logsView.currentRun": "Current run",
   "logsView.lastRun": "Last run logs",
   "logsView.noRun": "No server session",
diff --git a/src/components/ui/collapsible.tsx b/src/components/ui/collapsible.tsx
new file mode 100644
--- /dev/null
+++ b/src/components/ui/collapsible.tsx
@@ -0,0 +1,18 @@
+"use client";
+
+import * as React from "react";
+import { Collapsible as CollapsiblePrimitive } from "radix-ui";
+
+function Collapsible(props: React.ComponentProps<typeof CollapsiblePrimitive.Root>) {
+  return <CollapsiblePrimitive.Root data-slot="collapsible" {...props} />;
+}
+
+function CollapsibleTrigger(props: React.ComponentProps<typeof CollapsiblePrimitive.Trigger>) {
+  return <CollapsiblePrimitive.Trigger data-slot="collapsible-trigger" {...props} />;
+}
+
+function CollapsibleContent(props: React.ComponentProps<typeof CollapsiblePrimitive.Content>) {
+  return <CollapsiblePrimitive.Content data-slot="collapsible-content" {...props} />;
+}
+
+export { Collapsible, CollapsibleTrigger, CollapsibleContent };
