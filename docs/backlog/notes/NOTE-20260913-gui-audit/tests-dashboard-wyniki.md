# Tests — przebudowa 2026-09-13

Punkt powrotu źródeł przed przebudową: cc081ec3dd80193d3467ef489bf8b6af6c8edda5, zawiera akcje worktree i All projects. Nowe zmiany Tests pozostają robocze na t3code/poprawki-gui i są widoczne przez HMR. Nie publikowano stabilnego GUI, nie restartowano kontrolera ani zarządzanych serwerów.

Ekran działa w kontekście pickera: jeden projekt lub wspólna tabela wszystkich projektów. Najpierw metryki Running, Queued, Failed results i Unverified sources. Kliknięcie metryki ustawia odpowiedni widok i filtry. Osobna sekcja pokazuje do pięciu aktywnych zadań, pozycję w kolejce oraz akcję anulowania; większa kolejka ma przejście do pełnej listy dostępnych aktywnych rekordów.

Latest results pokazuje ostatni zakończony, nieanulowany wynik dla kombinacji projekt + worktree + preset. Ponowne udane wykonanie zastępuje wcześniejszy błąd; anulowanie nie ukrywa wcześniejszego wyniku. History zachowuje wszystkie dostępne wykonania. Obie listy mają wyszukiwanie, filtry projektu (All projects), worktree, presetu, wyniku, weryfikacji źródeł i okresu 24h/7d/30d oraz paginację po 10. Zmiana filtra resetuje stronę, a skrócenie danych ogranicza jej indeks.

Wynik polecenia pochodzi z source.processOutcome; faza queued/running ma pierwszeństwo. Końcowa faza kolejki może być failed przy kodzie procesu 0 ze względu na dirty_source. Nowy widok pokazuje wtedy Passed oraz osobno Unverified. Szczegóły nadal udostępniają surową ocenę kolejki, kod wyjścia, błąd, komendę, nazwy zmiennych i dowody źródeł. Current wymaga zgodnych obserwacji czystych źródeł, zgodnego HEAD oraz świeżych metadanych worktree; inny HEAD to Older commit, a niekompletne dane to Unverified. Ta informacja nie jest dowodem niezmienności całej zawartości repozytorium podczas testu.

Logi przeniesiono do shadcn Sheet otwieranego z wiersza. Zamknięcie zwraca fokus do przycisku. Formularz Run test to shadcn Dialog z projektem, worktree i presetem; zmiana celu odtwarza stan presetu. Przypisanie profilu środowiska pozostaje w zaawansowanych opcjach. Akcje serwera Start/Stop/Switch usunięto z Tests. Użyto istniejących Card, Button, Table, Badge, Input, Select, Tabs, Dialog i Sheet oraz ikon Lucide. Zachowano PL/EN.

Weryfikacja żywego GUI: wyłącznie istniejący tab_a, autoryzowana sesja. Sprawdzono All projects, tabelę po 10, statusy polecenia i źródeł, szczegóły z logami, formularz uruchamiania oraz brak przepełnienia całej strony. Narzędzie zrzutów wbudowanego podglądu nadal nie daje screenshotu; pomiary DOM i interakcje działają. Załączone PNG pochodzą z Playwright z danymi fixture, nie z żywego kontrolera.

Ograniczenia: istniejące API zwraca do 20 ostatnich uruchomień na projekt. Paginacja i metryki dotyczą dostępnej historii; starsze rekordy poza tym limitem nie są pobierane. Ekran ujawnia to w opisie. Czas jest liczony względem ostatniego odczytu dashboardu/metryk, bez nowego timera ani subskrypcji. Brak nowych endpointów i zmian cyklu życia kontrolera. Wcześniej przygotowany backend metryk worktree nadal nie został aktywowany.

Weryfikacja: 34/34 testy UI (PL/EN, 390/1440 px, filtry, paginacja, kolejka, fokus panelu, prawidłowy cel formularza i jeden kanał SSE); build i check kod 0; check obejmuje lint/typecheck, 310 testów jednostkowych i 7 testów zasobów. Jedno wcześniejsze ostrzeżenie unused b w tests/e2e/managed-test-queue.spec.ts pozostaje poza tą zmianą. Pełnego zestawu E2E z kontrolerem nie uruchamiano; wcześniejszy roboczy plik tego zestawu wymaga aktualizacji selektorów do nowych widoków. Wyniki testów przeglądarkowych i identyfikatory kolejki zapisuje tests-dashboard-verification.json. Kolejka kwalifikuje czystość źródeł jako dirty_source; kod procesu i ocena źródeł są raportowane osobno.

Cofnięcie tej serii: w roboczym katalogu najpierw git apply --reverse --check dla tests-dashboard.patch.txt, następnie git apply --reverse. Łatka obejmuje wyłącznie tę przebudowę ponad cc081ec, wraz z nowymi plikami. Nie obejmuje package.json, next.config.ts, artefaktów ani wcześniejszego roboczego testu E2E. Nie usuwać zbiorczo zmian użytkownika przez reset --hard. Dokumentację zapisano osobnym commitem na main zgodnie z regułami backlogu.
