--- a/src/features/projects/project-card.tsx
+++ b/src/features/projects/project-card.tsx
@@ -19,0 +20 @@
+import { Input } from "@/components/ui/input";
@@ -56,0 +58,10 @@
+  const [query, setQuery] = useState("");
+  const [page, setPage] = useState(0);
+  const search = query.trim().toLowerCase();
+  const filteredWorktrees = worktrees.filter((worktree) =>
+    `${worktree.branch ?? "detached HEAD"} ${worktree.path} ${worktree.head}`.toLowerCase().includes(search),
+  );
+  const pageSize = 10;
+  const pageCount = Math.max(1, Math.ceil(filteredWorktrees.length / pageSize));
+  const currentPage = Math.min(page, pageCount - 1);
+  const visibleWorktrees = filteredWorktrees.slice(currentPage * pageSize, (currentPage + 1) * pageSize);
@@ -257,0 +269,11 @@
+          <div className="mb-3 space-y-2">
+            <Label htmlFor={`worktree-search-${project.id}`}>{t("project.searchWorktrees")}</Label>
+            <Input
+              id={`worktree-search-${project.id}`}
+              type="search"
+              value={query}
+              onChange={(event) => { setQuery(event.target.value); setPage(0); }}
+              placeholder={t("project.searchWorktreesHint")}
+              className="max-w-lg"
+            />
+          </div>
@@ -270 +292 @@
-                {worktrees.map((worktree) => {
+                {visibleWorktrees.map((worktree) => {
@@ -308,0 +331,3 @@
+                {visibleWorktrees.length === 0 && (
+                  <tr><td colSpan={5} className="px-4 py-8 text-center text-muted-foreground">{t("project.noMatchingWorktrees")}</td></tr>
+                )}
@@ -310,0 +336,10 @@
+          </div>
+          <div className="mt-3 flex flex-wrap items-center justify-between gap-3">
+            <p className="text-xs text-muted-foreground" aria-live="polite" aria-atomic="true">
+              {t("project.worktreeResults", { from: filteredWorktrees.length ? currentPage * pageSize + 1 : 0, to: Math.min((currentPage + 1) * pageSize, filteredWorktrees.length), count: filteredWorktrees.length, total: worktrees.length })}
+            </p>
+            <nav aria-label={t("project.worktreePagination")} className="flex items-center gap-2">
+              <Button type="button" variant="outline" size="sm" disabled={currentPage === 0} onClick={() => setPage(currentPage - 1)}>{t("project.previousPage")}</Button>
+              <span className="text-xs tabular-nums">{t("project.worktreePage", { page: currentPage + 1, pages: pageCount })}</span>
+              <Button type="button" variant="outline" size="sm" disabled={currentPage + 1 >= pageCount} onClick={() => setPage(currentPage + 1)}>{t("project.nextPage")}</Button>
+            </nav>
--- a/src/i18n/messages.ts
+++ b/src/i18n/messages.ts
@@ -116,0 +117,8 @@
+  "project.searchWorktrees": "Szukaj worktree",
+  "project.searchWorktreesHint": "Gałąź, ścieżka lub commit…",
+  "project.noMatchingWorktrees": "Brak pasujących worktree.",
+  "project.worktreeResults": "{from}–{to} z {count} wyników · łącznie {total}",
+  "project.worktreePagination": "Strony listy worktree",
+  "project.worktreePage": "{page} / {pages}",
+  "project.previousPage": "Poprzednia",
+  "project.nextPage": "Następna",
@@ -446,0 +455,8 @@
+  "project.searchWorktrees": "Search worktrees",
+  "project.searchWorktreesHint": "Branch, path or commit…",
+  "project.noMatchingWorktrees": "No matching worktrees.",
+  "project.worktreeResults": "{from}–{to} of {count} results · {total} total",
+  "project.worktreePagination": "Worktree list pages",
+  "project.worktreePage": "{page} / {pages}",
+  "project.previousPage": "Previous",
+  "project.nextPage": "Next",
--- a/tests/ui/dashboard.spec.ts
+++ b/tests/ui/dashboard.spec.ts
@@ -4,0 +5,48 @@
+for (const width of [390, 1440]) {
+  test(`worktree search and pagination preserve the operation target at ${width}px`, async ({ page }) => {
+    await page.setViewportSize({ width, height: 1000 });
+    const data = dashboardFixture();
+    const initial = data.projects[0].worktrees[0];
+    data.projects[0].worktrees = Array.from({ length: 25 }, (_, i) => ({
+      ...initial, path: `/fixture/checkout-${i}`, branch: `feature-${i}`, head: `${i.toString(16).padStart(8, "0")}abcdef`, shortHead: i.toString(16).padStart(8, "0"),
+    }));
+    data.projects[0].project.selectedWorktreePath = data.projects[0].worktrees[0].path;
+    const { requests, errors } = await mountDashboard(page, data);
+    const rows = page.locator("tbody tr");
+    const pagination = page.getByRole("navigation", { name: "Worktree list pages" });
+    const previous = pagination.getByRole("button", { name: "Previous", exact: true });
+    const next = pagination.getByRole("button", { name: "Next", exact: true });
+    await expect(rows).toHaveCount(10);
+    await expect(previous).toBeDisabled();
+    await next.click();
+    await expect(rows.first()).toContainText("feature-10");
+    await next.click();
+    await expect(rows).toHaveCount(5);
+    await expect(next).toBeDisabled();
+    await page.getByRole("button", { name: "Select worktree feature-24", exact: true }).click();
+    const search = page.getByRole("searchbox", { name: "Search worktrees" });
+    await search.fill("FEATURE-1");
+    await expect(rows).toHaveCount(10);
+    await expect(previous).toBeDisabled();
+    await expect(pagination).toContainText("1 / 2");
+    await search.fill("checkout-7");
+    await expect(rows).toHaveCount(1);
+    await expect(rows).toContainText("feature-7");
+    await search.fill("00000018");
+    await expect(rows).toHaveCount(1);
+    await expect(rows).toContainText("feature-24");
+    await search.fill("no-such-worktree");
+    await expect(page.getByText("No matching worktrees.", { exact: true })).toBeVisible();
+    await expect(previous).toBeDisabled();
+    await expect(next).toBeDisabled();
+    await expect(page.locator("#worktree-web")).toContainText("feature-24");
+    await search.fill("");
+    await expect(rows).toHaveCount(10);
+    await expect(page.locator("#worktree-web")).toContainText("feature-24");
+    expect(await page.evaluate(() => document.documentElement.scrollWidth)).toBeLessThanOrEqual(width);
+    await page.screenshot({ path: test.info().outputPath("worktree-pagination.png"), animations: "disabled", fullPage: true });
+    expect(requests).toEqual([]);
+    expect(errors).toEqual([]);
+  });
+}
+
