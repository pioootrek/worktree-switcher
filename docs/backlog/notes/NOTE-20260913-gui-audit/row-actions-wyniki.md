# Akcje w wierszach worktree — 2026-09-13

Na żądanie właściciela zapisano poprzedni stan źródeł w commicie `7ae66f20e1bddd5f8dfad1518e10220a00ff61f1` na `t3code/poprawki-gui`: sidebar shadcn, metryki, paginacja, wyszukiwanie, filtry i przygotowany backend metryk. Commit pomija lokalne package.json/next.config.ts, artefakty i wcześniejszą roboczą zmianę testu e2e. Bez push i scalenia do main.

Nowa seria pozostaje robocza do obejrzenia. Ostatnia kolumna Table zawiera jeden przycisk outline: Uruchom, Otwórz albo Przełącz tutaj. Shadcn DropdownMenu zawiera Szczegóły (Dialog), blokadę oraz Restart/Zatrzymaj tylko dla działającego worktree. AlertDialog przy przełączeniu wskazuje bieżącą gałąź, cel i port. Wymuszone zwolnienie rezerwacji agenta zachowuje osobne potwierdzenie. Rezerwacje, prunable i trwające operacje blokują mutacje; szczegóły pozostają dostępne. Lucide oznacza akcje, wszystkie nowe etykiety mają wersje PL/EN.

Usunięto kółka selekcji, dolny picker celu, pasek operacji, powtórzone szczegóły i stopkę wybranego worktree wyłącznie z sekcji Worktrees. Tests/Resources/Logs zachowują swój istniejący wybór celu. API operacji i rezerwacji dostaje ścieżkę klikniętego rekordu, niezależnie od tego wyboru. Nie dodano odczytów ani subskrypcji. Otwórz buduje URL z hostname dashboardu, portu i TLS projektu, bez kopiowania tokenu/parowania; dostępność tego adresu zależy od istniejącego nasłuchu i sieci.

Weryfikacja przez kolejkę MCP: build kod 0, 29 testów UI kod 0, lint kod 0 z jednym wcześniejszym ostrzeżeniem tests/e2e/managed-test-queue.spec.ts. Faza kolejki failed kwalifikuje dirty_source, nie błąd procesów. Testy obejmują dokładny cel start/switch/restart/stop, anulowanie i zatwierdzenie przełączenia, URL bez tokenu, stany zarezerwowane/uruchamiane/prunable, dialog i powrót fokusu, polski/angielski oraz szerokości 390/768/1440. Nie ponawiano całej jednostkowej baterii backendu, bo ta seria zmienia GUI.

W istniejącej karcie wbudowanej przeglądarki zweryfikowano WinPath (10 widocznych wierszy, brak dolnego pickera, menu Szczegóły/Blokada i szczegóły właściwej gałęzi). Prosty Prawnik pokazał Otwórz oraz menu Szczegóły/Restart/Stop/Blokada. Nie wykonano operacji na żywych serwerach; te przetestowano z API fixture. Przywrócono WinPath/Worktrees i przewinięto na górę. Brak przepełnienia całej strony. Na telefonie szeroka tabela przewija się poziomo do akcji. Snapshot wbudowanej przeglądarki nadal niedostępny; załączone obrazy pochodzą z fixture.

Cofnięcie tylko tej serii: w worktree najpierw `git apply --reverse --check` dla row-actions.patch.txt, następnie `git apply --reverse`. Reverse check przeszedł. Bazowy commit umożliwia porównanie stanu sprzed zmiany. Nie resetować pozostałych lokalnych plików. Nowego kontrolera nie uruchomiono; wcześniejsze oczekiwanie na osobno uzgodniony restart backendu metryk pozostaje aktualne.
