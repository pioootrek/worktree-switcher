# Resources — 2026-09-13

Właściciel polecił uporządkować Resources analogicznie do Worktrees i Tests. Punkt powrotu źródeł: 0644dfb. Zmiany działają lokalnie przez HMR i pozostają robocze do oceny. Nie restartowano kontrolera ani zarządzanych serwerów; nie zmieniano limitów hosta.

Przed zmianą ekran All projects miał 5295 px wysokości przy szerokości 1195 px. Powielał sterowanie serwerem, wybór celu operacji i osobny wybór pomiaru dysku. Trzy karty projektów mieszały bieżące zużycie, historyczne maksimum RAM i pełne listy katalogów.

Nowa hierarchia: cztery karty RAM teraz, CPU, całkowity dysk i .next, następnie zakładki Storage oraz Servers. Karty prowadzą do odpowiedniej tabeli i sortowania. Picker projektu ogranicza cały ekran; All projects daje jedną tabelę i sumy w wybranym zakresie. Bieżące RAM/CPU sumują dostępne pomiary aktywnych zarządzanych serwerów, z liczbą pokrytych serwerów. Dane całego hosta i testów nie są doliczane. CPU 100% oznacza jeden rdzeń. Brak aktywnych serwerów jest komunikowany wprost; brak pomiaru pozostaje kreską. Historyczne/stare pomiary nie stają się bieżącym zużyciem.

Tabela Storage zawiera projekt, gałąź i ścieżkę, total, .next, node_modules, czas/status pomiaru oraz Details. Sortowanie: największe worktree, .next, node_modules, nazwa. Wyszukiwanie po projekcie/gałęzi/ścieżce, filtry projektu i stanu pomiaru, paginacja po 10 z resetem i ograniczeniem indeksu. Brak rekordu pomiaru dla odkrytego worktree tworzy wyłącznie prezentacyjny stan Not measured. Poprzednie pomiary pozostają widoczne podczas skanowania lub po błędzie, z oznaczeniem stanu i datą. Rozmiar .next zawiera .next/cache i jest już częścią total — tych wartości nie dodajemy do siebie. Sumy dotyczą wpisów projektów; nie są pomiarem całego dysku ani gwarantowanym miejscem do odzyskania.

Tabela Servers zawiera projekt, aktywne worktree, stan, RAM, CPU, procesy i wiek/status pomiaru. Obsługuje wyszukiwanie, projekt, aktywne/nieaktywne, sortowanie RAM/CPU/nazwa i paginację. Szczegóły serwera zachowują wykres i maksimum RAM. Szczegóły dysku pokazują rozbicie, historię i największe katalogi w shadcn Sheet; zlikwidowano drugi picker i powieloną listę. Użyto istniejących Card, Button, Table, Tabs, Badge, Select, Input, Sheet, AlertDialog, Alert i ikon Lucide. Wykresy zachowują istniejące SVG, z kolorami z tokenów motywu.

Usuwanie .next i odświeżanie są akcjami konkretnego rekordu, z projectId oraz dokładną ścieżką. Usuwanie wymaga potwierdzenia pokazującego pełną ścieżkę; jest blokowane dla aktywnego serwera, rezerwacji, skanowania, aktywnych/oczekujących testów i niedostępnego katalogu. Blokada jest ponownie sprawdzana przy potwierdzaniu; backend nadal pozostaje autorytetem. Błąd operacji jest widoczny w panelu. Żadnych zbiorczych operacji usuwania, automatycznych skanów ani nowych timerów/subskrypcji. Nowe API nie było potrzebne.

Żywy podgląd sprawdzono wyłącznie w istniejącym tab_a. Nowy ekran ma około 1394 px, 38 worktree, 71,4 GiB total i 34,0 GiB .next; tabela Servers pokazuje dwa działające serwery i zatrzymany WinPath. Brak przepełnienia strony przy 1195 px. Sprawdzono szczegóły pomiaru oraz zasoby procesu, bez usuwania katalogów lub uruchamiania/wyłączania serwerów. PNG to kontrolowane dane Playwright; wbudowane narzędzie screenshot pozostaje niedostępne.

Końcowy build kod 0, 37/37 testów UI; check kod 0 (313 jednostkowych + 7 zasobów), końcowy lint kod 0 z wcześniejszym ostrzeżeniem unused b w pliku E2E. Testy nowych przepływów obejmują 390/1440 px, PL/EN, nieznane pomiary, paginację, cel operacji pomiędzy projektami, anulowanie potwierdzenia, blokadę aktywnego worktree i widoczny błąd odświeżenia. Wyniki poleceń i testów zapisano w resources-verification.json. Pierwszą serię UI przerwano celowo po dopracowaniu prezentacji błędów, aby przebudować statyczny eksport przed ponowną weryfikacją. Nie zaliczamy przerwanego uruchomienia jako sukcesu. Pełnego controller E2E nie uruchamiano; wcześniejszy roboczy test E2E pozostaje poza tą zmianą.

Cofnięcie: w roboczym worktree wykonać git apply --reverse --check dla resources.patch.txt, a po poprawnym wyniku git apply --reverse. Łatka dotyczy tylko Resources ponad 0644dfb; konfiguracja package.json/next.config.ts, artefakty i wcześniejszy E2E są wyłączone. Dokumentacja na main jest zapisywana osobno od roboczego kodu GUI.
