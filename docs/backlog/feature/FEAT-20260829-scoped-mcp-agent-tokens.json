{
  "area": "core",
  "created": "2026-08-29",
  "id": "FEAT-20260829-scoped-mcp-agent-tokens",
  "links": {
    "related_ids": [
      "FEAT-20260829-independent-auth-modes",
      "FEAT-20260829-launch-environment-profiles",
      "FEAT-20260829-multi-project-worktree-switching",
      "NOTE-20260829-readiness-assessment"
    ]
  },
  "notes": [
    {
      "author": "user",
      "date": "2026-08-29",
      "text": "Support creating access tokens for LLM clients and decide which environment-configuration operations should be available through MCP."
    },
    {
      "author": "agent:codex",
      "date": "2026-08-29",
      "text": "Token creation is a human authority boundary. MCP clients may inspect their own effective identity and capabilities but must not mint another token or escalate their scopes."
    },
    {
      "author": "agent:codex",
      "date": "2026-09-05",
      "text": "Reprioritized under the 2026-09-05 owner-first, reusable-experiment direction. Revisit when distinct client/project permissions are needed. Preserve the existing authentication and session ownership until then; compact status and owner-authorized runtime tools can ship independently."
    }
  ],
  "priority": "later",
  "problem": [
    "The MCP listener currently uses one persistent bearer token shared by every local client. That is sufficient for the initial fixed read-and-claim tool set, but it cannot safely distinguish agents, restrict projects, revoke one client, or authorize higher-impact persistent configuration changes."
  ],
  "risk": {
    "dimensions": [
      "auth-rbac",
      "data-exposure",
      "process"
    ],
    "level": "high",
    "rollback": "Disable scoped configuration mutations and retain a rotated read-and-claim-only local token during migration. Revocation must take effect without restarting managed project servers."
  },
  "schema_version": 1,
  "scope": [
    "Replace the single shared MCP credential with human-created agent tokens carrying a stable client identity, display label, creation time, optional expiry, project allowlist, and explicit capabilities.",
    "Define least-privilege capabilities including projects:read, claims:manage, profiles:select, and config:write. A token receives only the capabilities selected by the human creator; configuration write is never implied by ordinary claim access.",
    "Create, list metadata for, rotate, and revoke tokens through the authenticated human dashboard and CLI. Never expose token minting, raw token listing, scope escalation, or revocation of other clients as MCP tools.",
    "Generate at least 256 bits of entropy, show the raw token exactly once, persist only a verifier/hash plus a non-secret identifier and short prefix, and compare credentials in constant time.",
    "Let an MCP client read its own identity, project scope, capabilities, and expiry without returning its credential. Bind MCP sessions, reservations, configuration mutations, and audit events to that client identity.",
    "Support immediate revocation, optional expiration, last-used metadata, and clear client errors for expired, revoked, project-out-of-scope, or capability-denied requests without leaking which unrelated projects exist.",
    "Provide copyable private MCP client configuration for a newly created token and document that it belongs in the agent client's secret store, never AGENTS.md, CLAUDE.md, repository files, logs, or prompts.",
    "Define a migration from the existing owner-only mcp-token file: existing installations remain operable long enough to create a scoped token, then can explicitly disable and remove the legacy credential."
  ],
  "source": "Product discussion on 2026-08-29",
  "status": "open",
  "title": "Issue scoped MCP access tokens for coding agents",
  "trigger": [
    "Revisit when distinct client/project permissions are needed. Preserve the existing authentication and session ownership until then; compact status and owner-authorized runtime tools can ship independently."
  ],
  "type": "feature",
  "validation": [
    "Create two agent tokens with different project allowlists and verify each can discover and act only on its allowed projects without learning identifiers or status of the other project.",
    "Verify projects:read cannot claim or configure, claims:manage cannot edit profiles, profiles:select cannot create or change a profile, and config:write permits only the configuration operations defined by the service layer.",
    "Verify raw credentials never appear in SQLite, API/MCP status, audit events, controller/project logs, errors, process arguments, or a second token-list response.",
    "Revoke an active client's token and verify its next MCP request and session renewal fail immediately while its existing agent lease follows the documented expiry/release policy.",
    "Verify expired, malformed, unknown, and legacy-disabled tokens fail with indistinguishable authentication responses and constant-time verifier comparison.",
    "Verify MCP cannot mint tokens, grant itself scopes, expand its project allowlist, or reveal environment and secret values even when it holds config:write."
  ],
  "value": [
    "Each coding agent gets an independently revocable least-privilege identity, making MCP configuration access auditable without turning one leaked shared token into full control of every project."
  ]
}
