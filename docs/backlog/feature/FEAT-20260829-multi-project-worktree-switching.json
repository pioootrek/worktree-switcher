{
  "area": "core",
  "created": "2026-08-29",
  "id": "FEAT-20260829-multi-project-worktree-switching",
  "notes": [
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "One Switch instance must operate several projects concurrently; switching one project must not stop the others."
    },
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "Managed development servers have resource priority. Worktree Switcher must be optimized and effectively unnoticeable in idle use; persistence may begin with a file behind a replaceable service layer."
    },
    {
      "author": "agent:codex",
      "date": "2026-08-29",
      "text": "Architecture selected: one lightweight Node controller serves a statically exported Next.js UI and manages processes; versioned JSON is accessed through a ProjectStore adapter; distribution uses the worktree-switcher npm package and foreground CLI."
    },
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "Decision state: proposed. Discuss visible reservations that pin a server to a worktree and an MCP interface through which an LLM can inspect runtime placement, acquire a reservation, and release it."
    },
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "Use SQLite from the MVP instead of JSON persistence now that reservations, MCP clients, and audit history require atomic state changes. Keep persistence behind the service boundary."
    },
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "Use shadcn/ui for the dashboard GUI."
    },
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "Approved: reservations are MVP scope; controller shutdown stops its owned managed processes; dirty worktrees remain runnable with a visible warning."
    },
    {
      "author": "agent:codex",
      "date": "2026-08-29",
      "text": "Functional prototype implemented: static Next.js/shadcn dashboard, loopback Node controller, SQLite StateStore with transactional audit records and exclusive human reservations, Git porcelain discovery, per-project lifecycle serialization, readiness checks, bounded logs, SSE updates, strict mutation token/origin checks, and owned process-tree shutdown. Automated checks cover Git parsing, reservation exclusion/ownership, owned start-stop, and refusal to kill an unrelated port owner. The backlog remains open: configurable command presets, agent leases/MCP, full CLI, three-project switching validation, browser visual QA, and release resource acceptance are not complete. Measured controller RSS was about 67 MiB before and 68 MiB after a dashboard refresh on Node 24; a bare Node HTTP process on this host measured about 60 MiB, so the 50 MiB RSS target requires revisiting the runtime or metric."
    },
    {
      "author": "human:project-owner",
      "date": "2026-08-29",
      "text": "Make the controller reachable from the LAN and add a local firewall exception if required."
    },
    {
      "author": "agent:codex",
      "date": "2026-08-29",
      "text": "LAN access implemented with a secure boundary: default bind is 0.0.0.0, --host 127.0.0.1 remains available, startup prints a high-entropy pairing URL for the detected LAN address, all API reads/mutations and SSE require the token, and browser mutations require same-host Origin. UFW is active on the reference host, but its rules and changes require interactive sudo unavailable to the agent; the documented rule is restricted to 192.168.1.0/24 and TCP port 47831."
    },
    {
      "author": "agent:codex",
      "date": "2026-08-29",
      "text": "Node launch-command resolution now detects pnpm, npm, yarn, or bun and selects port passing by framework instead of appending a fixed pnpm argument sequence. Next.js and unknown Node servers receive PORT; Vite, Astro, Nuxt, and Angular receive --port through package-manager-specific forwarding. A schema migration replaces the prototype's invalid pnpm dev -- --port command, and registration rejects a missing dev script early. Custom presets for non-Node and unusual frameworks remain open scope."
    }
  ],
  "priority": "now",
  "problem": [
    "Developers manually stop and restart development servers when moving between Git worktrees, and the workflow becomes error-prone when several related repositories run at the same time."
  ],
  "risk": {
    "dimensions": [
      "availability",
      "process"
    ],
    "level": "medium",
    "rollback": "Disable process mutation and retain read-only worktree discovery if lifecycle management cannot reliably isolate projects and terminate complete child process trees."
  },
  "schema_version": 1,
  "scope": [
    "Create a local control plane that registers multiple Git repositories, discovers their worktrees, and maintains one independently switchable server slot with a stable port for each project.",
    "Include serialized lifecycle operations per project, readiness checks, isolated logs, loopback-only control endpoints, and reconciliation after control-plane restart.",
    "Ship a static Next.js dashboard composed from owned shadcn/ui source components inside one lightweight Node controller and isolate SQLite persistence behind a replaceable transactional StateStore contract.",
    "Include human hard locks and expiring agent leases pinned to worktrees, allow dirty worktrees with a warning, and stop only controller-owned process trees during shutdown."
  ],
  "source": "Product planning conversation on 2026-08-29",
  "status": "open",
  "title": "Run and switch multiple worktree-backed projects",
  "type": "feature",
  "validation": [
    "Run at least three fixture projects concurrently, switch each to another worktree, and verify that only the selected project's process restarts while every project keeps its configured port.",
    "Verify concurrent requests for one project are serialized, failures are isolated, stale paths and arbitrary commands are rejected, and the dashboard reports readiness and logs accurately.",
    "Measure the release build on the supported Linux reference environment and verify at most 50 MiB idle RSS, negligible idle CPU, bounded log memory, and no repository-wide watcher or continuous Git polling.",
    "Verify concurrent reservation attempts are atomic, at most one active reservation exists per project, expired leases can be reclaimed, audit events commit with their mutation, and a failed transaction leaves no partial state.",
    "Verify keyboard navigation, visible focus, reduced-motion behavior, and screen-reader announcements; runtime and reservation states must remain understandable without color.",
    "Verify dirty worktrees run with a persistent warning, controller shutdown terminates every owned process tree, and an unrelated process occupying a configured port is reported but never terminated."
  ],
  "value": [
    "A developer can move an entire local stack between branches from one dashboard without repeatedly navigating directories or disturbing unrelated services."
  ]
}
